import type{ABInput,ABResult}from"./types";export function evaluateContextAB(i:ABInput):ABResult{const score=(i.candidateReturn-i.baselineReturn)*40+(i.candidateQuality-i.baselineQuality)*0.3+(i.candidateSuccessRate-i.baselineSuccessRate)*0.5;const winner=score>2?"candidate":score<-2?"baseline":"tie";return{winner,scoreDelta:Number(score.toFixed(2)),promotionAllowed:false,reasons:[winner==="candidate"?"candidate 우세":"baseline 유지 또는 동률","8-12에서는 자동 승격 금지"],strategyVersion:"phase8-context-ab-v8.12"}}